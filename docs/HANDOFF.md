# Goedu-Split 작업 인계 (HANDOFF)

어느 플랫폼(Codex, Claude Code, Kilo 등)이든 이 파일과 git 기록만으로 이어서 작업할 수 있게 유지한다.
완료 조건 하나를 끝낼 때마다 **테스트 → 이 파일 갱신 → 로컬 커밋** 순서로 남긴다. 끝에 몰아서 쓰지 않는다.

- 마지막 갱신: 2026-09-25 KST (노트북 접기, 1.0.4 앱 빌드·설치·릴리스)
- 작업 브랜치: `work/1.0.3-hardening`
- 마지막 체크포인트 커밋: 이 파일을 마지막으로 바꾼 커밋 (`git log -1 -- docs/HANDOFF.md`로 확인)

## 최종 목표

NEIS 정오표·문항정보표 기반 성취평가 분석과 **추정분할점수 설계**를 인터넷 없이 교사 PC에서 안전하게 쓰는 Windows·macOS 앱.
시험지 오류 점검과 외부 AI는 교사가 명시적으로 켜는 선택 기능으로만 붙인다.

## 단계 로드맵

| 단계 | 내용 | 추천 모델 / 강도 | 상태 |
|---|---|---|---|
| 1 | 작업 보존, 내보내기 가명화, CSV 수식 차단, CI 테스트, 이 인계 파일 | Claude Code `claude-opus-5-5` 또는 Codex `gpt-6-sol` / high | 완료, CI 통과, Windows 실기 미검증 |
| 2 | 예측-실측 보정 리포트 (교사 예상정답률 vs 실제 수준별 정답률) | `gpt-6-sol` / medium | 구현 완료(맥), 실제 시험 자료 검토 전 |
| 3 | 시험지(HWP/HWPX/PDF)에서 문항 번호·배점·유형만 로컬 추출 | `gpt-6-sol` / high, 패키징 설계만 `gpt-6-astra` / high | 맥 구현 완료. kordoc은 앱에 동봉하지 않음, 실제 시험지 미검증 |
| 4 | 선택형 온라인 검토 모듈 (로컬 규칙 점검 → 복사-붙여넣기 브리지 → Jev 전송 게이트) | 설계·보안 `gpt-6-astra` / high → 구현 `gpt-6-sol` / high | 대기 |
| 부채 | React 원본 소스 복구, `app/main_window.py` 분할 | 필요할 때만 | 대기 |

- 매 단계 완료 판정은 구현과 다른 모델 계열이 맡는다(효과는 미측정).
- ultra는 비권장: 작업이 한 파일에 몰려 자동 위임 분리 실익이 작다.

## 1단계 완료 조건과 진행

- [x] 1. `work/1.0.3-hardening`에 기존 1.0.3 미커밋 작업 커밋 (`c43cc52`). stash 2개는 그대로 둠.
      stash@{0}의 코드·테스트는 `archive/exam-issue-2026-07` (`ed7a27a`)에 xlsx 없이 보존.
- [x] 2. 이 인계 파일 생성
- [x] 3. 내보내기 기본값 가명화 (`app/export_privacy.py`, 테스트 `tests/test_export_privacy.py`)
      - 가명 번호는 분석 자료마다 한 번 무작위(SystemRandom)로 섞어 매긴다(`_student_pseudonyms`). 같은 분석의 CSV·근거 엑셀·계산기는 같은 가명을 쓴다.
        학생 수가 1000명 이상이면 자릿수를 늘려 글자 순서와 숫자 순서가 같게 한다.
      - 결과 CSV `학생결과.csv`: 기본 `가명 ID`, 행도 가명 순서. 실명은 확인란 + 경고 확인 후에만.
      - 근거 엑셀: 저장 전 선택 창, 기본 가명(행도 가명 순서), 실명은 확인란 + 경고.
      - 계산기로 보내는 분석자료(`_build_spliter_evidence_payload`): 항상 가명, 원본 파일은 폴더 없이 파일 이름만.
        학생 목록도 가명 순으로 정렬한다. 계산기는 근거자료를 받을 때마다 새 무작위 시드로 표본을 다시 뽑고(`I()` = 시각+난수),
        기본 방식(`difficultyAverage`)은 평균만 쓰므로 순서를 바꿔도 추정의 재현성이 나빠지지 않는다.
        계산기 화면의 근거 학생 표시와 계산기 '작업 저장' JSON도 가명이 된다. 상담 모드를 켜도 실명이 보이던 문제도 함께 해소.
      - 그래프 PNG: 학생 이름 없음(확인), 화면 그래프 저장 버튼 없음.
      - 포트폴리오: 파일 내보내기 경로 없음. 내부 스냅샷(앱 저장 폴더, 실명)은 과목 간 연결에 필요해 유지 — 후속 결정 필요.
- [x] 4. CSV 수식 주입 차단 (`= + - @` 탭·CR 시작 값 앞에 `'`). 근거 엑셀도 수식처럼 보이는 문자열을 글자로 저장하도록 보완
      (openpyxl 기본값은 수식 `f`임을 확인). 분할점수 검토표의 기존 방어는 그대로.
- [x] 5. `.github/workflows/windows-build.yml`: 소스 개인정보 감사 → Node 22 설정 → Python 테스트 → 계산기 웹 테스트 → (빌드) → 빌드 결과 개인정보 감사.
      - 테스트는 명령마다 단계를 나눴다. GitHub Actions의 `cmd` 단계는 마지막 명령의 종료 코드만 본다.
      - Python 테스트는 `QT_QPA_PLATFORM=offscreen`, `PYTHONIOENCODING=utf-8`. 파일 인코딩 문제를 가리지 않도록 `PYTHONUTF8`은 쓰지 않았다.
      - 소스 감사는 테스트 전에 둔다(테스트가 만드는 `__pycache__`를 감사 스크립트가 차단 경로로 보기 때문).
      - README의 `windows_release_audit.py --source .`는 소스 키트 폴더용이라 `.git`이 있는 체크아웃에서는 실패하므로 CI에 넣지 않았다.
      - 로컬 확인: YAML 파싱(시스템 python3의 PyYAML, Ruby) 통과, `git archive HEAD`로 꺼낸 깨끗한 소스에서 소스 감사 통과, Python 73·Node 43 통과.
      - **미검증: 실제 Windows 러너에서의 첫 실행.** push 승인 후 `workflow_dispatch`로 한 번 돌려 봐야 한다.
        주의: 입력 `checkout_ref` 기본값이 `main`이라 그대로 두면 main을 빌드하고 `missing: docs`로 소스 감사가 실패한다.
- [x] 6. Windows 실기 검증 체크리스트 작성 (아래). **실행은 사람 작업이며 아직 하지 않음.**

## 독립 검토 결과 (2026-09-24)

- 검토관: 맥락을 공유하지 않은 별도 Claude 서브에이전트, 읽기 전용. 다른 계열(GPT-6-Astra, GPT-5.6-Sol)은 opencodex 경로가 두 번 모두
  `model_not_found`(404)로 실패해 쓰지 못했다. **같은 계열 검토라는 한계가 있다.**
- 판정: 조건부 완료. 테스트는 보호 기능 8가지를 각각 끈 변형에서 모두 실패해 의미 있는 검증으로 인정됐다.
- 반영함: 가명이 명부 순서대로라 학급 열과 합치면 반/번호가 드러나는 문제 → 무작위 가명 + 파일 행 가명 순 정렬.
  CI 수동 실행의 `checkout_ref` 함정 → 체크리스트 0번에 명시. Node 20(지원 종료) → 22. 인계 파일 문구 정정.
- 사용자 결정 (2026-09-24, 원문 요지):
  1. push 승인 → `work/1.0.3-hardening`만 push. 보관 브랜치는 로컬 사용자 경로 때문에 push하지 않는다.
  2. 포트폴리오는 **학번 해시**로 변경 → 반영함(아래 '1단계 이후 추가 작업').
  3. 계산기 가명 표시는 유지하고 **교사가 버튼을 누르면 실명을 확인**할 수 있으면 된다 → '자료 > 가명 ↔ 실명 확인' 추가.
  4. 실제 자료 의심 파일은 바탕화면 `goedusplit-확인필요-자료/`로 옮김(사용자가 확인 후 답변 예정). stash 안 원본은 그대로 남아 있다.
- 기록만 함(범위 밖): 1-5등급 컷 '결과 복사'에 제외 학생의 반/번호가 클립보드로 들어간다(파일 아님).
- 기록만 함(범위 밖, 기존 문제): 근거 엑셀의 숨김 시트 `_앱내부데이터` JSON이 셀 한도 32767자에서 잘린다(30명·4문항에서도 발생).
  이 시트를 읽는 코드는 없다. 실명 모드에서는 숨김 시트에도 실명이 들어간다.
- 수정분 재검토(같은 방식의 별도 Claude 서브에이전트): '부분 해결' — 계산기 자료가 명부 순서로 남아 있었고 테스트 공백 3건, 1000명 이상 정렬 문제.
  모두 반영했고, 지적된 변형 3개와 정렬 제거 변형이 이제 테스트에서 실패하는 것을 확인했다.

## 1단계 이후 추가 작업 (2026-09-24)

- **계산기 '작업 저장'·'CSV 내보내기'가 데스크톱 앱에서 파일을 남기지 않던 문제 확인·수정.**
  - 재현: 실제 Qt WebEngine(offscreen, 격리 프로필, 합성 자료)에서 두 버튼 모두 `downloadRequested`는 오지만 파일 0개.
    앱이 요청을 받아 주지 않아 Qt가 취소했다. 1.0.1 이전부터 있던 문제로 보인다.
  - 수정: `_connect_spliter_downloads`(프로필당 한 번 연결) → 저장 위치를 묻고 `accept()`. 계산기 페이지가 아닌 요청과 취소는 거절.
    저장된 CSV는 수식처럼 보이는 칸만 `'`로 막는다(숫자 `-5`는 그대로, BOM·줄바꿈 유지).
  - 수정 후 같은 재현: 두 파일 모두 고른 위치에 생성, 작업 JSON에 합성 문항 18개 보존.
  - 미검증: 계산기 '작업 불러오기'(파일 선택)를 Qt에서 왕복해 보는 것, Windows.
- 포트폴리오 스냅샷(version 2): 학번·반/번호·이름 대신 `student_hash`(HMAC-SHA256, 24자)만 저장.
  키는 이 PC 앱 설정 `privacy/portfolio_hash_key`(32바이트, 처음 쓸 때 생성). 예전 version 1 실명 스냅샷도 같은 해시로 연결해 읽는다.
  화면의 이름·반/번호는 지금 불러온 분석 자료에서만 되찾고, 없으면 `학생#abc123`. 설정 초기화·다른 PC면 이전 스냅샷과 연결이 끊긴다.
  이 Mac의 기존 스냅샷은 0개였다. 다른 PC의 예전 실명 스냅샷 파일은 그대로 실명이 남아 있다.
- '자료 > 가명 ↔ 실명 확인': 현재 분석의 가명·학급·반/번호·이름을 화면 창에만 보여 준다. 파일 저장 없음.
- 테스트: Python 85, Node 43 통과. 새 보호 기능(CSV 점검, 해시 저장, 다른 페이지 거절)을 끄면 테스트가 실패하는 것 확인.

## 2단계: 예측-실측 보정 리포트 (2026-09-24, 맥)

- 진입: 예상정답률 탭 '자료 > 예측-실측 비교'. 계산기의 현재 작업(검토안 평균)을 분석 결과와 비교한다.
  시험 전 작업을 '작업 불러오기'로 연 뒤 쓰는 흐름이다.
- 계산: `app/calibration.py`(순수 함수, `tests/test_calibration.py` 7개).
  - 실측 = 각 분할점수 **위아래로 가장 가까운 학생**(전체의 약 10%, 최소 3명, 10점 이내, 양쪽 같은 수, 동점 포함)의 정답률이며
    **위쪽 평균과 아래쪽 평균을 같은 비중**으로 합친다. 한쪽에만 학생이 있으면 요약에 '참고만' 표시.
  - 처음 구현(수준 안 하위 1/3)은 경계 집단이 모두 분할점수 위라 정확한 예측도 '낮게 예측'으로 보이는 치우침이 있었다(독립 검토 지적).
    IRT 모의 자료(300명·25문항, 씨앗 10개, 기준 = 분할점수 위아래 가장 가까운 점수의 정답률 동일 비중)에서
    이전 방식 평균 치우침 +2.0~+3.9%p(전 수준 한쪽) → 현재 방식 -0.7~+0.3%p, 최대 2.2%p.
  - 수준 전체 평균은 칸 툴팁과 엑셀에 참고로 둔다.
  - 선택형은 문항별, 서답형은 정오표에 학생별 총점만 있어 묶음(배점 가중 예측 vs 서답 총점 비율)으로 비교.
  - 요약은 아래 '실제 자료 비교와 설계 수정'에서 **문항·난이도별 상대차** 중심으로 바뀌었다(수준별 평균 차이는 적용 분할점수에 묶여 정보가 없음).
    경계 학생 5명 미만이면 '참고만'. 서답형은 시험의 모든 서답형에 예측값이 있을 때만 묶음 비교(아니면 안내), 분석에 없는 서답형은 '찾지 못한 문항'.
    수행평가 합산 시 '수준은 합산, 정답률은 지필' 안내.
  - 순환성: 실제 수준은 적용한 분할점수로 나눈 결과라 참고 자료로만 표시.
- 엑셀 저장: 요약·문항별 시트, 학생 식별정보 없음(테스트로 확인).
- 확인: 실제 Qt로 비교 창을 그려 보았다(계산기 웹 엔진은 빼고 기존 작업 가져오기 경로 대신 같은 작업 데이터를 넘김).
  같은 캡처 스크립트에서 분석 자료+계산기 로딩이 멈췄으나, 검증된 재현 스크립트에 분석 자료를 넣으면 정상 로딩되어 스크립트 문제로 판단.
- 기준표 제안은 아래 '다음 시험 기준표 제안'에서 구현했다.
- 미검증: 실제 시험 자료로 결과가 교사에게 의미 있는지(바탕화면 실제 자료로 사용자가 직접 확인 예정).

## 실제 자료 비교와 설계 수정 (2026-09-24)

- 사용자 요청으로 바탕화면 실제 자료를 **로컬 스크립트로만** 처리하고 집계만 출력했다(이름·학번·학생별 행 출력 없음, 원자료를 대화로 보내지 않음).
  스크립트는 작업용 임시 폴더에만 있고 저장소에는 넣지 않았다.
- 자료: 공통수학1, 382명(바탕화면 103명 파일은 같은 시험의 일부로 보임), 선택형 18·서답형 2, 총점 100.
  `예상추정분할점수조회`에는 문항별 예측이 없고 경계별 예상 분할점수(평균·최소·최대)만 있다.
- 발견 1(설계 결함): 경계 학생 실측으로 계산한 분할점수는 수식상 적용 분할점수와 거의 같다(실제 자료 5개 경계 모두 차이 1점 이내).
  경계 학생의 평균 총점이 곧 분할점수이기 때문이다. 따라서 '수준별 평균 차이'는 예측 정확도가 아니라 '검토안 분할점수 - 적용 분할점수'였다.
  → 비교 창을 **문항·난이도별 상대차**(경계 평균 차이를 뺀 값) 중심으로 바꾸고, 위쪽 표는 검토안/적용/경계 실측 분할점수로 정직하게 표기.
- 발견 2: 382명 자료에서 목표 A(어려운) 문항도 E/미도달 경계 학생이 평균 22% 정답(5지선다 찍기 약 20%).
  기본 기준표의 낮은 수준 값(목표 A의 D 10, E 0 등)이 현실보다 낮다. 목표 C 문항도 D·E 경계가 기본값보다 10%p 이상 높았다.
  (문항별 목표수준이 없어 '경계 학생 2/3 이상 정답인 가장 낮은 수준'으로 가정한 예시. 참고용)
- 발견 3(기존 버그): PySide6 6.11에서 `runJavaScript`가 JavaScript 객체를 `''`로 돌려줘, `_fetch_spliter_project`를 쓰는
  **NEIS 입력표 '계산기 현재값 불러오기'와 예측-실측 비교가 실제 앱에서 항상 '값 없음'**이었다. JSON 문자열로 받아 해석하도록 고쳤다(`_parse_spliter_project`).
  단위 테스트가 딕셔너리를 직접 넘겨 드러나지 않았고, 실제 계산기 끝까지 재현으로 찾았다.

## 다음 시험 기준표 제안 (후보 1)

- 비교 창의 '다음 시험 기준표 제안': 같은 목표수준(계산기 문항의 targetLevel) 선택형 문항들의 경계 학생 실측 평균(0.5 올림).
  목표수준별 2문항 이상일 때만 제안, 경계 학생 없는 칸은 현재값 유지. 기준표 규칙으로 정리한 값을 화면에 보여 주고 규칙으로 바뀐 칸은 `*`와 툴팁.
  고른 행만 저장(`_apply_preset_suggestion`), 계산기 기준표도 갱신하되 이번 작업 표는 재계산하지 않음.

## 시험지에서 문항 가져오기 (후보 2 = 3단계)

- `app/exam_structure.py`: HWPX(zip XML)·PDF(pypdf)는 내장 읽기, HWP는 로컬 kordoc(이 맥에 2.9.0 설치돼 있음)을 `KORDOC_OFFLINE=1`,
  셸 없이, UTF-8로 실행. kordoc 마크다운 이스케이프·표를 줄로 펼침. 문항 머리(`1.`, `[서답형 1]` 등)를 번호 순서로만 인정하고
  `2) 조건`(배점 없는 괄호 번호)·`서답형 1~2번은` 같은 오탐을 막는다. 배점·보기 수·누락 번호·총점 경고.
- UI: '자료 > 시험지에서 문항 가져오기' → 미리보기(배점 수정·포함 체크) → 확인 후 계산기 작업을 교체(난이도 보통·목표 C·현재 기준표 값).
- 검증: 합성 시험지 텍스트·HWPX·PDF 모두 같은 구조, 실제 kordoc으로 HWPX 오프라인 읽기, 실제 계산기에 6문항·배점 반영 끝까지 확인.
- 미검증·한계: 실제 학교 시험지(HWP 바이너리, 2단 편집 PDF, 수식·그림 보기, 표 배치). kordoc은 앱에 동봉하지 않아
  kordoc 없는 PC는 HWPX/PDF만 가능. kordoc의 PDF는 `pdfjs-dist`가 없어 실패하므로 PDF는 내장 읽기만 쓴다.

## 독립 검토(ac23951, 판정 '미완료') 반영 (2026-09-24)

- [높음] Windows `kordoc.cmd`는 cmd.exe를 거쳐 `&`·`%` 등을 해석 → 시험지를 임시 폴더에 `input.<확장자>`로 복사해 넘기고,
  임시 경로에 cmd 특수문자가 있으면 실행 거부. 원래 파일명은 kordoc에 전달하지 않는다.
- `KORDOC_OFFLINE`은 kordoc 2.9.0에 없는 설정이라 제거. 문구를 '인터넷을 쓰는 kordoc 옵션(수식 OCR 모델 내려받기 등)은 켜지 않음'으로 정정.
- [버그 수정 부작용] 계산기 기본 예시 18문항(`N번`, 목표 E/D/C/B/A 순, 나머지 빈칸)을 `_is_sample_calculator_project`로 알아본다.
  비교 전에는 확인을 받고(기본 '아니오'), NEIS 창을 열 때의 자동 불러오기에서는 건너뛴다. 실제 계산기 기본 상태가 감지되는 것 확인.
- 기준표 제안: 기준표는 '보통' 기준이라 문항 난이도 보정(쉬움 +10, 어려움 -10)을 빼고 평균. 화면 값은 규칙 정리를 더 바뀌지 않을 때까지 적용한 값
  (한 번 적용으로는 약 2%에서 저장값과 달랐음) → 무작위 3,000건으로 '보이는 값 = 저장값' 확인. 경계 학생이 적거나 한쪽뿐인 경계는 경고.
- 요약: 난이도 그룹이 하나면 항상 참인 '비슷하게 예측' 문장 대신 안내. 일부 문항만 예측·수행평가 합산 시 분할점수 직접 비교 문장 생략.
- 파서: `[1]`·`문 1.`·`1번.`, 표지 유의사항 번호 제외(배점도 보기도 없으면 문항 아님), '서답형' 제목 아래 번호, `[서답형 1-1]` 소문항 합산,
  선택형 번호를 이어 쓴 서답형 1번부터 재번호, `<4점>`·`【3점】`·줄 끝 `4점`, `(1)~(5)` 보기, '선택형 1~N' 안내와 대조한 뒤쪽 누락 경고.
  HWPX 표·글상자 안 문단을 바깥 문단에 섞지 않음.
- 시험지 읽기 오류: 권한 오류도 안내, 대기 커서 항상 복구. 시험지 가져오기 때 근거 자료는 기존 NEIS 흐름처럼 현재 분석을 함께 보냄
  (근거를 빼면 아래 계산기 멈춤과 같은 경로로 계산기가 멈춰서 되돌림).
- **[중대, 기존 버그] 분석 후 '작업 불러오기'로 계산기가 멈춤**: 분석을 먼저 하고 계산기 탭을 연 상태(흔한 순서)에서 근거 없는 작업 JSON을 불러오면
  React `removeChild` 오류로 계산기가 죽고 작업이 사라졌다(예측-실측 비교의 정상 흐름). 원인은 앱 주입 스크립트가 React가 관리하는 요소의
  `textContent`를 덮어써 글자 노드를 바꿔치기한 것(`polishText`, `updateViewContext`의 근거 미리보기 안내). 글자 노드는 두고 `nodeValue`만 바꾸도록 수정.
  `tests/manual_calculator_flows.py --run`(합성 자료, 실제 Qt WebEngine)이 옛 코드에서 실패·새 코드에서 통과함을 확인. 노트북 화면 회귀 검사 4종 통과.
- 남은 한계(미처리): kordoc 실행 중 화면 멈춤(아래 재검토 반영으로 90초에서 하위 프로세스까지 중단), 2단 편집에서 한 줄에 두 문항, `[단답형 1]`과 `[서술형 1]`을 함께 쓰는 시험지,
  배점 없는 `1)` 머리, 계산기 작업 교체 뒤 웹 되돌리기 기록, `tests/manual_laptop_ui.py`의 project() 조회도 같은 '' 문제 가능성.

## 수정분 재검토(390d272, 판정 '조건부 완료') 반영 (2026-09-24)

- 계산기 멈춤 수정은 검토관이 옛 코드(실패 3건)와 새 코드(통과)로 재현 확인. 추가 조작(행 클릭·검토안 추가·근거 재전송·재불러오기)도 오류 없음.
- [회귀] 줄 끝 `N점`(괄호 없음)이 문항 본문 숫자(예: '평균 점수가 72점')를 배점으로 덮어씀 → 괄호·'배점' 표기를 우선하고,
  괄호 없는 값은 다른 배점이 없을 때만 쓰며 '확인하세요' 표시. `배점: 100`을 10으로 읽던 문제도 수정.
- 예시 판정에 기본 배점(4,4,5,…,8,8)·난이도 규칙 추가. 정답률만 바꾼 작업은 여전히 예시로 보일 수 있으므로 NEIS 창 자동 불러오기를 건너뛸 때
  창 안내문에 '자동으로 불러오지 않았습니다 … 계산기 현재값 불러오기를 누르세요'를 표시(실제 Qt 확인).
- Windows kordoc 시간 초과: `subprocess.run`은 cmd.exe만 죽이고 node가 파이프를 잡아 무한 대기 가능 → `Popen` + 새 프로세스 그룹,
  90초 초과 시 Windows `taskkill /T /F`, 그 외 `killpg`. 임시 폴더 정리 오류는 무시(잘못된 '권한' 안내 방지).
- 문항 수 안내('선택형 1~N번', 'N문항')는 첫 문항 앞(표지)에서만 읽는다(서답형 머리 `[서답형 3번]`·본문 '서술형 3문항' 오인 방지). 재번호된 서답형은 안내도 맞춰 뺀다.
- '서답형' 제목 아래 배점 없는 번호 문항도 유지(배점 없음 경고). 수동 흐름 검사는 경고창도 막고 임시 폴더를 자동 정리.
- 남은 한계: 배점 괄호가 있는 표지 유의사항(예: '1. … 선택형 20문항(70점)')은 여전히 문항으로 잡힐 수 있다.

## 사용자 테스트 피드백 반영 (2026-09-24)

- 사용자: PDF는 잘 되지만 HWP가 잘 안 됨 / 가명 창 정렬 요청 / 가져오기 후 난이도 전부 보통·목표 C.
- 실제 시험지(바탕화면 HWP·PDF, 같은 시험)는 로컬에서만 읽고 한글을 '가'로 가린 구조만 확인했다(내용은 대화로 보내지 않음).
- HWP 원인: 문항 번호가 한글 **자동 번호**라 kordoc 출력에 번호 글자가 없다. 번호 문항이 보기 묶음보다 훨씬 적으면
  **보기(①) 묶음마다 선택형 1문항**으로 나누고(보기 사이 빈 줄 허용), '채점 기준'이 나오는 곳 앞에서 멈춘다. 남은 질문 줄은 표지의
  'N문항' 수만큼 서답형으로, 배점은 채점 기준표의 '논술형 k' 블록에서 두 번째 숫자 줄(첫 단계 점수 다음의 문항 배점)을 읽는다(확인 표시).
- 배점이 빠진 문항이 하나이고 나머지 합이 100점 미만(차이 20점 이하)이면 100점에서 나머지로 채우고 확인 표시(PDF 서답형 2번).
- 결과: 실제 HWP·PDF 모두 선택형 18·서답형 2, 합계 100점, 문항정보표와 번호·배점 20/20 일치.
- 난이도: 왼쪽 문항정보표(없으면 불러온 분석의 문항)에서 가져오되, 배점이 80% 이상 맞을 때만 사용(다른 시험 표 방지, 맞지 않으면 안내).
  문항정보표가 없으면 배점 하위 1/3 쉬움·상위 1/3 어려움. 목표수준은 난이도 규칙(쉬움 E, 보통 C, 어려움 B)만 사용
  (불러온 분석이 지난 시험일 수 있어 번호별 정답률은 쓰지 않음). 미리보기에서 난이도·목표를 목록으로 수정, 난이도를 바꾸면 목표가 따라 바뀜.
  성취기준도 문항정보표에서 채운다. 실제 앱: 문항정보표 있을 때 쉬움 9·보통 8·어려움 3(표와 동일), 목표 E 9·C 8·B 3.
- 가명 ↔ 실명 창: 열 제목 클릭으로 오름·내림차순 정렬, 학급·반/번호는 숫자 기준('1/2' 다음 '1/10').
- 테스트: Python 137, Node 43, 계산기 흐름 검사 4/4.

## 사용자 테스트 피드백 2차 (2026-09-24)

사용자 원문 요지: 한글 시험지는 잘 가져와짐. 목표는 A~E여야 하고 난이도(쉬움·보통·어려움)가 아님, 예측/실측 비교에서 다시 볼 것.
정답 인원수를 바꿔도 상단 추정 분할점수가 바뀌지도 나타나지도 않음. 예상정답률 탭을 누르면 다른 앱처럼 상단 탭 이름이 모두 바뀜.

- **목표수준(A~E)**: 새 `calibration.observed_target` = 기준표(목표 설정)의 목표수준별 줄(난이도 보정·규칙 적용 후,
  즉 `_target_level_rates`) 가운데 문항의 **경계 학생 실측 정답률과 가장 가까운 줄**(최소제곱, 경계 3곳 이상). 기준표 줄을 쓰는 이유:
  가져온 문항은 그 줄로 기본 예상정답률을 채우므로 실측 패턴을 가장 잘 재현한다.
  - 실제 자료(382명): 쉬움 9문항의 실측 목표가 A~E에 흩어짐 → 난이도 규칙(쉬움→E)은 맞지 않음. 실측 목표 A 5·B 3·C 6·D 3·E 3(서답형 2 포함).
    기본 기준표로 가져온 추정 분할점수와 NEIS 예상추정분할점수의 평균 차이: 난이도 규칙 약 12점 → 실측 목표 약 5.5점.
  - 비교한 다른 규칙(경계 정답률 50%/67% 이상인 가장 낮은 수준, NEIS 자동의 수준 전체 50%)은 참고로만 확인. NEIS 입력표 자동 불러오기
    (`_item_default_target_level`, 수준 전체 50%)는 **바꾸지 않았다** — 정의가 둘인 상태, 통일은 후속 결정.
  - 가져오기: 불러온 분석이 시험지와 문항 수·번호·배점(80%)이 맞으면 미리보기에 '실제 응답으로 목표수준 정하기' 확인란.
    시험지 글에 과목명(NEIS 학점 표기 '(4)' 제거)이 있으면 기본 켬. 끄면 난이도 규칙(임시, 안내 문구). 서답형은 문항별 응답이 없어 서답형 묶음 결과.
  - 예측-실측 비교 창: '실측 목표' 열(다르면 색), 요약에 '목표수준이 실측과 다른 선택형 n문항', 엑셀 '문항별'에 '실측 목표수준' 열(고정 열 G).
- **분할점수 줄 원인 2가지**: (1) 표의 정답 인원 칸은 누를 때마다 0명부터 순환 → A가 B보다 낮아지는 순간 `summarize`의 역전 검사가
  모든 분할점수를 오류 문구로 바꿈(20명이면 원하는 값까지 거의 계속 안 보임). (2) 오른쪽 선택 문항 패널의 O/X 편집은 감시 대상이 아니었고,
  표 칸 숫자 변화는 텍스트 노드 변경이라 `closest`가 없어 무시됨.
  → 미리보기만 `summarize(project, {allowInversions: true})`로 계속 계산하고 빨간 경고(예: '선택형 1번(A<B)')를 붙임.
  NEIS 표·저장·예측-실측 비교 검사는 그대로 엄격. 감시는 `.table-panel, .detail-panel, .side-panel`과 텍스트 노드의 부모까지.
- **탭 이름이 바뀌던 원인**: 창 폭 1180~1439에서 계산기 탭에 들어가면 입력 패널이 자동으로 접히고, 넓어진 탭 막대 폭으로 이름 세트를
  고르므로 약 1430~1439에서는 모든 탭이 긴 이름으로 바뀜. 패널은 다른 탭으로 가도 돌아오지 않았음(합성 자료로 재현).
  → 계산기 때문에 접힌 동안은 패널이 있는 것처럼 이름을 고르고(`_sidebar_hidden_for_calculator`), 계산기를 떠나면 패널을 다시 펼침.
  사용자가 직접 접은 경우(`_sidebar_user_choice`)는 그대로.
- 확인: 실제 자료(바탕화면, 로컬 처리, 집계·화면 구조만 출력)로 분석 → 계산기 탭 → HWP 가져오기 → 비교 창까지 앱 흐름 재현.
  테스트 Python 150, Node 43, 계산기 흐름 검사 6/6. 새 테스트는 각 수정을 되돌리면 실패하는 것을 확인.
- 독립 검토(맥락 없는 별도 Claude 서브에이전트, 읽기 전용, 같은 계열 한계) 판정 '조건부 완료' → 모두 반영:
  1. 다른 시험 분석으로 목표가 기본 설정될 위험(과목명 부분 일치 '수학'⊂'공통수학1', 빈 배점을 분석 자료로 채운 뒤 비교해 늘 일치)
     → 배점 일치는 시험지에서 읽은 배점으로만 계산, 확인란 기본 켬은 과목명(단어 경계, 로마 숫자 포함)·학년도·학기가 모두 시험지에
     있을 때만(`_paper_names_loaded_exam`). **같은 학기 다른 차수(1차·2차)는 여전히 구분 못 함** — 확인란 문구로 안내. 학생 0명·실측 없음이면 확인란 숨김.
  2. 서답형 묶음을 '보통' 줄과 비교 → 구성 문항 난이도별 줄의 배점 가중 평균과 비교(`members`).
  3. `_sidebar_hidden_for_calculator`가 남는 문제 → 1180 미만·수동 펼치기/접기/끌기에서 해제.
  4. 확인란 전환이 교사가 직접 고른 목표를 덮어씀 → 직접 고른 행은 확인란·난이도 변경에도 유지.
  테스트 보강: 앱의 실제 기준표 규칙으로 각 줄이 자기 목표로 돌아오는지, 서답형 묶음, 과목·학기 대조, 미리보기 확인란, 좁은 창·수동 조작.

## 초보자 관점 UI/UX 점검과 반영 (2026-09-24~25)

점검: 합성 시험(200명, 18+2문항)을 `run_analysis`에 넣어 1320×860에서 모든 탭·대화상자를 offscreen 캡처(스크립트는 작업용 임시 폴더).
**주의**: 점검 스크립트는 QSettings와 앱 데이터 폴더(`_ai_material_root_dir`)를 둘 다 격리해야 한다. 처음에 후자를 빠뜨려
사용자가 저장한 실제 포트폴리오(해시 ID·점수)가 캡처에 한 번 나왔다(파일 수정 없음, 캡처는 휴지통).

사용자 요청 "1~14 모두 실행"으로 반영:
1. 첫 실행 탭 이름이 가장 짧은 세트 → 창 크기 이벤트가 탭 막대 배치보다 먼저 와서 생김. 탭 막대 Resize 이벤트 필터로 다시 고름.
2·3. '처음 시작하기' 번호 중복·제목과 설명 붙음, '시험지…후속 버전' 문구 → 수정(시험지 가져오기 위치 안내).
4. '분석 실행' 버튼이 스크롤 맨 아래 → 입력 패널 아래에 고정(`btn_run_analysis`). 입력 패널은 이제 스크롤 영역을 감싼 QWidget.
5. 분석 전 빈 그래프·필터 → `data_results`로 묶어 분석 후에만 표시.
6. 문항 표가 문18부터 → 원인은 `install_frozen_columns`: 고정 열 보조 표가 같은 모델을 쓰며 정렬을 켤 때 Qt 기본(내림차순)으로 재정렬. 보조 표 정렬 표시를 먼저 맞춤.
7. 그래프 축 제목 잘림 → `_MarginKeepingCanvas`: 설계 크기보다 작을 때만 tight_layout(처음 시도한 '설계 여백 인치 유지'는 작은 칸에서 막대가 납작해져 버림).
   그래프 글꼴에 없는 σ는 SD로. 모니터링 입력칸은 스크롤, 그래프 최소 높이.
8. 상단 아이콘 버튼에 글자(입력 패널·상담 모드·화면 크기·기본·밝기, `iconLabel` 속성), 계산기 파일 버튼에 글자(불러오기·CSV·저장).
9. 정오 칸 표기(. = 정답, 숫자 = 오답 번호, 빈칸 = 무응답) 안내와 머리글 툴팁.
10. 계산기 '3/3 · 99.0%' → 분석 정답률이 직접%로 들어가 생김. O/X와 다른 칸은 '직접 99.0%'로 표시(`data-goedu-direct` + CSS, React 텍스트 노드는 건드리지 않음).
11. 신뢰도·정답률·변별도 등 풀이(머리글 툴팁, 부제), 변별도 그래프 제목 쉬운 말로.
12. 예측-실측 비교 창: '먼저 볼 것'(`calibration.headline_lines`, 크게 빗나간 칸 3개 + 할 일) 맨 위, 긴 설명은 접기, 표 읽는 법 한 줄.
13. 모니터링 전국·전년도 칸: 0을 '미입력'으로 표시(원래도 0은 미입력으로 판정), 무엇을 어디서 넣는지 안내.
14. 포트폴리오 탭 위 전체 경로 제거(툴팁과 '저장 위치' 버튼으로).
- 테스트: 새 `tests/test_beginner_ux.py` 12개(주요 5개 수정을 되돌리면 실패 확인). 전체 Python 162, Node 43, 계산기 흐름 6/6.
- 독립 검토(별도 Claude 서브에이전트, 읽기 전용) '조건부 완료' → 반영:
  H1 tight_layout이 남기는 자리표시 레이아웃 엔진 때문에 첫 맞춤 뒤 여백이 굳어 7번이 되살아남 → 맞춘 뒤 엔진을 비우고, 판단은 생성 시 한 번.
  테스트를 '작게→크게→더 작게'로 바꿈. M1 '미입력' 평균(0)에 표준편차만 넣으면 0과 비교해 'Ⅲ 점검' → 평균·표준편차 둘 다 있어야 판정.
  L1 '0/0 · 72%' 칸도 '직접' 표시. L2 모니터링 표 문구의 σ도 SD·'표준편차의 n배'로. L3 160% 배율에서 상단 줄 폭 → 실제로 들어가는지 재서
  안 들어가면 '화면 크기'·'기본'·'밝기' 글자만 뺌(입력 패널·상담 모드 글자는 유지). 측정: 100~160% × 1180~1440px 모두 들어감.
- 미확인: 다크 모드, Windows, 실제 macOS 글꼴 렌더링. 계산기 '직접' 표시는 캡처로만 확인(자동 테스트 없음).

## 노트북 화면 접기와 1.0.4 앱·릴리스 (2026-09-25)

사용자 결정: 탭 안의 큰 영역 전부 접기(기본 펼침), GitHub는 main 병합 + 릴리스, 버전 1.0.4, 설치 위치 /Applications.
- `FoldSection`: 각 탭의 요약·그래프·표·안내 영역 머리에 ▼ 제목. 접으면 그 높이를 펼쳐진 가장 큰 옆 영역에 넘기고(QSplitter는 스스로 안 넘김),
  펼치면 되찾음. 상태는 설정 `ui/fold/<key>`에 저장. 테스트 `test_large_areas_fold_give_their_room_away_and_remember_it`.
- **설치 앱에서 HWP가 안 되던 문제**(빌드 후 발견): kordoc은 `#!/usr/bin/env node` 스크립트라 Finder로 연 앱의 최소 PATH에서
  `env: node: No such file`로 실패. kordoc 폴더·/opt/homebrew/bin·/usr/local/bin을 PATH에 더해 실행(`_kordoc_env`). 최소 PATH로 실제 HWP 20문항·100점 확인.
- 빌드: `build_scripts/build_mac.sh`(테스트 164, Node 43, 개인정보 감사, ad-hoc 서명). 스크립트는 `dist/Goedu-Split.app`뿐 아니라 PyInstaller 중간
  폴더 `dist/Goedu-Split`이 있어도 멈춘다 — 재빌드 전 둘 다 치울 것. `pack_mac.sh`는 `rm -rf`를 쓰므로 같은 과정을 임시 폴더에서 직접 수행(ditto + hdiutil).
  최소 환경 실행 20초 이상 확인. 7월 1.0.1 빌드는 `dist.old.<시각>/`에 보관.
- 설치: `/Applications/Goedu-Split.app` 1.0.4(서명 검증 통과). Windows 산출물은 CI run 36138580095(b43bab3)에서 받음.
- SHA256은 README 표. 공개 저장소이므로 main 병합 전 전체 범위 gitleaks 검사.

## 기타 확인 (2026-09-24)

- 계산기 '작업 불러오기' 왕복: 실제 Qt에서 보이는 '작업 불러오기' 버튼을 마우스로 클릭 → 파일 선택 호출(.json 허용) →
  표식을 넣은 저장 작업이 계산기에 반영됨. (JavaScript `.click()`으로는 브라우저가 파일 선택을 막아 재현되지 않는다.)
- 로컬 원격 주소를 새 저장소 이름 `piman-code/goedusplit.git`으로 바꿨다(사용자 승인).

## 다음 첫 작업

1. (사용자) 실제 학교 시험지 1~2개(HWP·HWPX·PDF)로 '시험지에서 문항 가져오기'를 써 보고 틀린 문항 번호·배점을 알려 주기. 결과로 파서 조정.
2. (나중) Windows 실기 체크리스트(아래 1~8번). 사용자가 미뤄 둠.
3. 바탕화면 `goedusplit-확인필요-자료/` 파일은 **실제 학생 자료**로 확인됨(2026-09-24 사용자). 사용자가 직접 테스트할 때까지 그대로 둔다.
   에이전트는 이 폴더와 stash 안 xlsx를 열거나 옮기거나 지우지 않는다. stash는 push 대상이 아니다.
4. 다음 후보: 기본 기준표 낮은 수준 값(찍기 정답률) 검토, kordoc 앱 동봉(패키징·Windows), 4단계 온라인 검토 모듈.

## 후속 단계로 넘긴 것 (1단계 범위 밖)

- AI 검토 CSV(`_export_ai_review_csv`)와 계산기 번들 내부 CSV 내보내기에는 수식 차단이 없다. AI 화면은 이번 버전에서 꺼져 있고 번들은 건드리지 않는 범위라 4단계·번들 복구 때 처리.
- 포트폴리오 실명 스냅샷 보관 방식(암호화 또는 학번 해시) 결정.
- 포트폴리오 저장 위치 안내 문구가 'Finder'로 고정 — Windows에서는 '파일 탐색기'가 맞음.
- CI의 기존 '글꼴·아이콘 준비' 단계는 두 명령이 한 `cmd` 단계에 있어 첫 명령(`fetch_fonts.py`) 실패가 가려질 수 있다.

## 검증 명령

```bash
QT_QPA_PLATFORM=offscreen .venv/bin/python -m unittest discover -s tests
node --test tests/test_expected_rate_web.cjs
```

1단계 시작 시점 기준: Python 65개, Node 43개 통과. 현재: Python 129개, Node 43개 통과.
실제 계산기 흐름: `.venv/bin/python tests/manual_calculator_flows.py --run`, 화면 배치: `.venv/bin/python tests/manual_laptop_ui.py --run`.

## 경계 (현재 유효한 사용자 승인)

- 허용: 로컬 브랜치 생성, 로컬 커밋, `work/1.0.3-hardening` push(2026-09-24 사용자 승인).
- 금지: 다른 브랜치 push, 배포, 코드서명, 패키지 설치, 네트워크 사용, 실제 학생자료 열람, `rm` 계열 삭제(휴지통만).
- 이번 단계에서 건드리지 않음: 시험지·AI 기능, React 번들, 계산 계약(`app/expected_rates.py`)과 저장 형식.
- 두 플랫폼이 같은 작업 폴더에서 동시에 작업하지 않는다.

## 보관 현황과 주의 파일

- `stash@{0}` (2026-07): 시험지 오류 점검 작업. 미추적 파일에 xlsx 4개 포함 — **실제 학생 자료로 확인됨**(사용자), 내용 미열람.
  `학생 답 정오표data(1학년).xlsx`, `지필평가 교과목별 일람표(공통수학1).xlsx`, `문항정보표(공통수학1) 샘플.xlsx`, `예상추정분할점수조회(공통수학1)1학기 1차.xlsx`.
  stash는 push되지 않지만 로컬 git 객체에 남아 있다. 처리 방법은 사용자 결정 필요.
- `stash@{1}` (2026-06-11): `app/spliter_ox_source/` React 원본(Vite+TS)이 있다. 이후 빌드 JS를 직접 수정해 왔으므로 복구 시 현재 번들과 대조가 필요하다.
- `sample_data/`의 `학생답 정오표(1학기 1차)data.xlsx`(실제 자료 의심)는 바탕화면 `goedusplit-확인필요-자료/`로 옮겼다. stash의 xlsx 4개도 같은 폴더에 복사(권한 600). 내용 미열람.
- `archive/exam-issue-2026-07` push 전: 실제 로컬 사용자명이 든 경로 `/Users/<로컬 사용자>/...`를 일반 이름으로 바꿀 것.
  위치: `app/main_window.py`(예시 문구), `build_scripts/generate_exam_issue_qa_samples.py`, `distribution/MANUAL_QA_CHECKLIST.md`,
  `distribution/USER_GUIDE.md`, `tests/test_exam_issue_review_gui_flow.py` 등. 코드·테스트 외에 문서 2개(`MANUAL_QA_CHECKLIST.md`, `QUALITY_AUDIT.md`)도 함께 보관됐다.
- 원격 `origin`은 GitHub 저장소다(공개 여부 미확인). push 전 개인정보 감사를 먼저 돌린다.

## Windows 실기 검증 체크리스트 (사람 작업)

에이전트가 Mac에서 대신할 수 없다. Windows PC에서 합성 자료로 확인한다.

0. CI 첫 실행(push 승인 후): GitHub Actions `Build Windows Distribution`을 `workflow_dispatch`로 실행하면서
   입력 `checkout_ref`에 `work/1.0.3-hardening`을 직접 적는다(기본값 `main`이면 이 브랜치가 아니라 main을 빌드한다).
   소스 감사·Python 테스트·웹 테스트·빌드 감사가 모두 초록인지, 실패하면 어느 단계인지 기록.
1. 빌드: `python -m PyInstaller --noconfirm --clean goedusplit.spec` 성공, `dist\Goedu-Split\Goedu-Split.exe` 실행. 창 제목의 버전이 1.0.3인지.
2. 첫 실행: SmartScreen 안내 문구, 한글 폰트, 창 크기 1280x800·1366x768에서 입력 패널 접기.
3. 불러오기: 한글 경로·공백 있는 폴더의 합성 정오표·문항정보표 불러오기, 분석 실행.
4. 예상정답률 탭: 화면 표시(WebEngine), 소수 정답률 입력, 저장·불러오기, NEIS 입력표 XLSX 저장 후 Excel에서 열기.
   계산기의 근거 학생 표시가 실명 대신 `학생 001` 형식인지, '자료 > 가명 ↔ 실명 확인'으로 실명이 보이는지.
   계산기 '작업 저장'·'CSV 내보내기'를 누르면 저장 창이 뜨고 파일이 생기는지, 저장한 작업을 '작업 불러오기'로 다시 여는지.
5. 결과 CSV(`Ctrl+E`): 확인란을 끈 채 저장 → `학생결과.csv`를 Excel로 열어 한글이 깨지지 않고 첫 열이 `가명 ID`인지.
   확인란을 켜면 경고 창이 뜨고, '아니요'면 아무 파일도 생기지 않는지, '예'면 학번·반/번호·이름이 들어가는지.
6. 근거 엑셀(자료 메뉴): 저장 전 선택 창이 뜨는지, 기본 저장 파일에 실명이 없는지, 실명 포함은 경고 확인 후에만 되는지.
7. 오프라인: 네트워크를 끊은 상태에서 1~6이 동일하게 동작.
8. 결과 기록: 이 파일의 '작업 기록'에 날짜·Windows 버전·항목별 결과를 적는다.

## 재개 프롬프트 (다른 플랫폼에서 붙여넣기)

```text
작업 위치: Goedu-Split 저장소 루트 (Mac: Projects/003-goedusplit)
먼저 읽기: docs/HANDOFF.md → git status, git log -5 → HANDOFF의 '다음 첫 작업'에 필요한 파일만.
상황: 이전 실행이 사용 한도 등으로 중간에 끊겼을 수 있다. 마지막 커밋 이후의 미커밋 변경은 버리지 말고, HANDOFF의 현재 상태와 대조해 끝난 것과 덜 끝난 것을 먼저 판정하라. 테스트를 한 번 돌려 현재 상태를 확인하라.
진행: HANDOFF의 최종 목표·현재 단계·완료 조건·경계를 그대로 따른다. 완료 조건 하나마다 테스트 → HANDOFF 갱신 → 로컬 커밋.
경계: push·배포·설치·네트워크·실제 학생자료 열람은 HANDOFF에 사용자 승인 기록이 없으면 하지 않는다. 다른 에이전트가 같은 폴더에서 작업 중이면 멈추고 알린다. 삭제는 휴지통만.
보고: 한국어로 이번에 끝낸 것 / 미검증 / 다음 첫 작업.
```

## 작업 기록

- 2026-09-23: 1단계 시작. 조건 1·2 완료.
- 2026-09-24: 조건 3·4 완료 (한 커밋). 새 테스트 8개, 보호 기능을 끄면 해당 테스트가 실패하는 것도 확인.
- 2026-09-24: 조건 5 완료 (로컬 확인만, Windows 러너 미실행).
- 2026-09-24: 조건 6 완료 (체크리스트 작성).
- 2026-09-24: 독립 검토(조건부 완료) 반영. 무작위 가명·행 정렬, CI 안내, Node 22. 테스트 Python 75·Node 43 통과.
- 2026-09-24: 수정분 재검토(부분 해결) 반영. 계산기 자료도 가명 순 정렬, 테스트 보강. Python 77·Node 43 통과.
- 2026-09-24: 사용자 결정 반영(학번 해시, 실명 확인 창), 계산기 저장 버튼 수정. Python 85·Node 43 통과. 브랜치 push.
- 2026-09-24: CI 1차(run 35884119567) 실패 — 소스 감사 통과, Python 테스트 83/85. 둘 다 테스트 쪽 문제:
  node 출력을 인코딩 지정 없이 읽어 Windows cp1252에서 한글 해석 실패, Codex 테스트가 실제 `/opt/homebrew/bin/codex` 존재에 의존.
  테스트를 고치고 재실행(결과는 아래 줄에 기록).
- 2026-09-24: 2단계 예측-실측 비교 구현, 작업 불러오기 왕복 확인. Python 92·Node 43 통과.
- 2026-09-24: 독립 검토(미완료) 반영, 분석 후 작업 불러오기 계산기 멈춤 수정, 계산기 흐름 검사 추가. Python 125·Node 43 통과. CI(390d272) 통과.
- 2026-09-24: 수정분 재검토(조건부 완료) 반영. Python 129·Node 43 통과, 계산기 흐름 검사 4/4.
- 2026-09-24: 실제 자료 비교(집계만), 비교 창 상대차 중심 재설계, 기준표 제안, 시험지 문항 가져오기, 계산기 값 가져오기 버그 수정. Python 113·Node 43 통과.
- 2026-09-24: 독립 검토(조건부 완료) 반영 — 경계 집단 양쪽 대칭·동일 비중, 서답형 매칭, 배점 가중 요약, 수행평가 안내,
  포트폴리오 스냅샷 해시 순 저장·해시 기준을 예전 키(학번·반/번호·이름)와 같게, 계산기 CSV에 BOM·원자적 저장·확장자 변경 대비. Python 95·Node 43 통과.
- 2026-09-24: CI 2차(run 35884627716, 커밋 7cb912a) **전 단계 성공**. Windows에서 Python 85·Node 43 통과(건너뜀 0),
  소스·빌드 결과 개인정보 감사 통과, `Goedu-Split-1.0.3-windows.zip`과 `-setup.exe` 생성(아티팩트 약 432MB, 14일 보관).
